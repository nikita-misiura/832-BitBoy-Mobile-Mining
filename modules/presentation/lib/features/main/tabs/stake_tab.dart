import 'package:flutter/widgets.dart';

class StakeTab extends StatelessWidget {
  const StakeTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('stake');
  }
}
