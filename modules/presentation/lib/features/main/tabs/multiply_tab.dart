import 'package:flutter/cupertino.dart';

class MultiplyTab extends StatelessWidget {
  const MultiplyTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Multiply');
  }
}
