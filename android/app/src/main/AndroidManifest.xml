<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="com.example.permission.SEND_STATS" />
    <uses-permission android:name="android.permission.SEND_BROADCAST" />
    <permission android:name="com.example.yourapp.permission.RECEIVE_BROADCAST"
        android:protectionLevel="normal" />

    <application
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:label="bitboy">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service android:name="io.bitboy.mobile.miner_launcher.services.stats.StatsService" />
        <receiver
            android:name="io.bitboy.mobile.miner_launcher.services.stats.StatsBroadcastReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="io.bitboy.stats_sender" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </receiver>
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT" />
            <data android:mimeType="text/plain" />
        </intent>
    </queries>
</manifest>
